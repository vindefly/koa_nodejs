import voxLogs from "../libs/voxLogs";

(function () {
    function init() {
        console.log(2222);
        console.log(4444);
        voxLogs.logs('index next1')
    }

    init();
    console.info("start name");
})();